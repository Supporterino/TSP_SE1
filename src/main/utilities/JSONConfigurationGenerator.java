package utilities;

import configuration.CrossoverType;
import configuration.MutationType;
import configuration.SelectionType;

public class JSONConfigurationGenerator {
    public void build(int scenarioID,
                      CrossoverType crossoverType, double crossoverRatio,
                      MutationType mutationType, double mutationRatio,
                      SelectionType selectionType, int maximumNumberOfIterations) {
    }
}